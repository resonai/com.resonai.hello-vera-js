{
  "name": "com.resonai.hello-vera-js",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy":"vite build && git push origin --delete gh-pages && git subtree push --prefix dist origin gh-pages"
  },
  "dependencies": {
    "@resonai/vera-sdk": "^0.0.9"
  },
  "devDependencies": {
    "vite": "^4.1.0",
    "@vitejs/plugin-basic-ssl": "^1.0.1"
  }
}
